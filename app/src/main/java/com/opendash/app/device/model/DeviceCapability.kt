package com.opendash.app.device.model

enum class DeviceCapability {
    ON_OFF,
    BRIGHTNESS,
    COLOR_TEMP,
    RGB_COLOR,
    TEMPERATURE_READ,
    TEMPERATURE_SET,
    HUMIDITY_READ,
    VOLUME,
    PLAY_PAUSE,
    MEDIA_NEXT_PREV,
    POSITION,
    LOCK_UNLOCK
}
